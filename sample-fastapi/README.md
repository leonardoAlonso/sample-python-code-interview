# Fast api base code for code interview

This project uses UV as dependency manager, so you need to install it first.

```bash
pip install uv
```

To insall the dependencies, run the following command:

```bash
uv sync
```

To run the project, run the following command:

```bash
uv run fastapi dev main.py
```
